(()=>{const y=document.getElementById('year');if(y)y.textContent=new Date().getFullYear();const c=document.getElementById('netbg');if(!c)return;const ctx=c.getContext('2d');let w=0,h=0,dpr=1,nodes=[];const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;function resize(){dpr=Math.min(devicePixelRatio||1,1.5);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=reduce?0:Math.min(45,Math.floor(w*h/28000));nodes=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){ctx.clearRect(0,0,w,h);if(!reduce){for(const p of nodes){p.x+=p.vx;p.y+=p.vy;if(p.x<-20||p.x>w+20)p.vx*=-1;if(p.y<-20||p.y>h+20)p.vy*=-1}for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,dist=Math.hypot(dx,dy);if(dist<145){ctx.globalAlpha=(1-dist/145)*.22;ctx.strokeStyle='#22d3ee';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}for(const p of nodes){ctx.globalAlpha=.45;ctx.fillStyle='#22d3ee';ctx.beginPath();ctx.arc(p.x,p.y,1.6,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(draw)}}addEventListener('resize',resize);resize();draw()})();
