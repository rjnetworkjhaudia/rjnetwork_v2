(()=>{const c=document.getElementById('netbg');if(!c)return;const x=c.getContext('2d');let W,H,n=[];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const count=innerWidth<640?20:Math.min(52,Math.floor(innerWidth/26));function resize(){W=c.width=innerWidth;H=c.height=innerHeight}function init(){n=Array.from({length:count},()=>({x:Math.random()*W,y:Math.random()*H,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18}))}function draw(){x.clearRect(0,0,W,H);n.forEach(a=>{a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>W)a.vx*=-1;if(a.y<0||a.y>H)a.vy*=-1});for(let i=0;i<n.length;i++)for(let j=i+1;j<n.length;j++){let a=n[i],b=n[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<140){x.strokeStyle='rgba(140,180,255,'+(.12*(1-d/140))+')';x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}n.forEach(a=>{x.fillStyle='rgba(140,180,255,.3)';x.beginPath();x.arc(a.x,a.y,1.5,0,Math.PI*2);x.fill()});requestAnimationFrame(draw)}resize();init();addEventListener('resize',()=>{resize();init()});if(!reduced)requestAnimationFrame(draw)})();